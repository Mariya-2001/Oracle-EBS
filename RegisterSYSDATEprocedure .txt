-- Register SYSDATE procedure in EBS --

is SQL Plus , Write the following:

SQL> CREATE OR REPLACE PROCEDURE MARIYA_PR_SYSDT (ERRBUF OUT VARCHAR2, RETCODE OUT NUMBER) IS 
  2  V_DT DATE;
  3  BEGIN
  4  SELECT SYSDATE INTO V_DT FROM DUAL;
  5  FND_FILE.PUT_LINE(FND_FILE.LOG, 'YOU CAN SEE LOG DETAILS HERE!');
  6  FND_FILE.PUT_LINE(FND_FILE.OUTPUT,'CURRENT DATE =' || V_DT);
  7  EXCEPTION
  8  WHEN OTHERS THEN
  9  FND_FILE.PUT_LINE(FND_FILE.OUTPUT,'CHECK YOUR PROGRAM: ' || ERRBUF || 'ERRCODE= ' || RETCODE);
 10  END;
 11  /

Procedure created.


REGISTER to EBS:
1. APPLICATION DEVELOPER --> CONCURRENT  --> EXECUTABLE--> WRITE THE REQUIRED DETAILS 

2. CONCURRENT PROGRAM --> WRITE THE REQUIRED DETAILS --> CLICK PARAMETERS 
 
4. CLICK SWITCH RESPONSIBILITY --> ADMINISTRATOR RESPONSIBILITY --> 
REQUEST GROUP --> VIEW --> QUERY BY EXAMPLE --> ENTER 
WRITTE NAME% --> VIEW --> QUERY BY EXAMPLE --> RUN
ADD NEW PROGRAM --> WRITE THE PROCEDURE NAME --> SAVE  

5. SWITCH RESPONSIBILITY --> MARIYA_RESP --> VIEW --> REQUEST --> SUBMIT NEW REQUEST
--> CLICK LOV AND SELECT THE REQUIRED PROGRAM --> SUBMIT --> CLICK FIND 
--> VIEW OUTPUT 